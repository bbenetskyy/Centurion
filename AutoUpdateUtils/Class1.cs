using System;

namespace AutoUpdateUtils
{
    public class Class1
    {
        /*
         * AutoUpdateHost
            Console.WriteLine("Starting AutoUpdate Host service");
            var infoChecker = new InfoChecker();
            infoChecker.GetNewVersionInfo();
            if (infoChecker.NewVersionAvailable &&
                MessageBox.Show("Newer version available. Do you want to make an update?", "TODO:bot name",
                MessageBoxButtons.YesNo) == DialogResult.OK)
            {
                infoChecker.
            }
            Console.WriteLine("Closing AutoUpdate Host Service");
            Console.ReadLine();
            */
    }
}
